const allowedTimezones=new Set(['Asia/Seoul','Asia/Tokyo','Asia/Shanghai','UTC']);
export function normalizeWorkspaceInput(input:any={}){const name=String(input.name||'').trim().replace(/\s+/g,' '),requested=String(input.code||name).trim().toLowerCase(),code=requested.normalize('NFKD').replace(/[^a-z0-9가-힣]+/g,'-').replace(/^-|-$/g,'').slice(0,24),timezone=allowedTimezones.has(String(input.timezone))?String(input.timezone):'Asia/Seoul';if(name.length<2||name.length>40)throw Object.assign(new Error('워크스페이스 이름은 2~40자로 입력해주세요.'),{status:400});if(code.length<2)throw Object.assign(new Error('워크스페이스 코드는 영문·숫자·한글 2자 이상이어야 합니다.'),{status:400});return {name,code,brandId:input.brandId?String(input.brandId):null,timezone,isTest:Boolean(input.isTest)}}
export function workspaceInsertPayload(organizationId:string,input:any){const value=normalizeWorkspaceInput(input);return {organization_id:organizationId,brand_id:value.brandId,name:value.name,code:value.code,status:'onboarding',service_stage:'setup',timezone:value.timezone,data_region:'ap-northeast-2',metadata:{test_data:value.isTest,created_from:'workspace_rail'}}}

export function workspaceUpdatePayload(input:any,current:any={}){const value=normalizeWorkspaceInput({...current,...input,brandId:input.brandId??current.brand_id,isTest:input.isTest??current.metadata?.test_data}),description=String(input.description??current.description??'').trim().replace(/\s+/g,' ').slice(0,240);return {brand_id:value.brandId,name:value.name,code:value.code,description:description||null,timezone:value.timezone,metadata:{...(current.metadata||{}),test_data:value.isTest,updated_from:'workspace_settings'},updated_at:new Date().toISOString()}}
